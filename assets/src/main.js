/** @format */

import App from './App.svelte'
import './styles/bootstrap.scss' // Add all bootstrap styles
import './styles/main.scss' // add default styles

const app = new App({
  target: document.body
})

export default app
